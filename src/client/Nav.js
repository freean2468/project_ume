import React, { Component } from 'react';
import './app.css';

export default class Nav extends Component {
  componentDidMount() {

  }

  render() {
    return (
      <nav className="Nav">
        nav
      </nav>
    );
  }
}
