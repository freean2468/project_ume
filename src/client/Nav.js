import React, { Component } from 'react';
import './app.scss';

export default class Nav extends Component {
  componentDidMount() {

  }

  render() {
    return (
      <nav className="Nav">
        nav
      </nav>
    );
  }
}
